<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="User">
	<select id = "idCheck" parameterType="java.lang.String" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM THEATERS_USERS
		WHERE USER_ID=#{id}
	</select>
	
	<insert id = "addUser" parameterType="bit.com.theaters.model.UserDto">
		INSERT INTO THEATERS_USERS ( USER_ID, USER_PWD, USER_PHONE, USER_EMAIL, USER_BIRTH, USER_NAME, USER_NICKNAME, USER_JOIN_DAY, USER_AUTH)
		VALUES(#{user_id}, #{user_pwd}, #{user_phone}, #{user_email}, #{user_birth}, #{user_name}, #{user_nickname}, SYSDATE, 3)
	</insert>
	
	<select id ="loginCheck" parameterType="bit.com.theaters.model.UserDto" resultType="bit.com.theaters.model.UserDto">
		SELECT USER_ID, USER_NICKNAME, USER_AUTH FROM THEATERS_USERS
		WHERE USER_ID=#{user_id} AND USER_PWD = #{user_pwd} 
	</select>
	
</mapper>